cmake_minimum_required(VERSION 3.12)
get_filename_component(PARENT_DIR ../ ABSOLUTE)
include(pico_sdk_import.cmake)
project(fight-key)

pico_sdk_init()

add_executable(
    fight-key
    src/main.cpp
    src/usb_descriptors.cpp src/report.cpp src/Hid.cpp
    src/Controller.cpp
)
include_directories(
    include
)

target_link_libraries(
    fight-key pico_stdlib pico_multicore tinyusb_board tinyusb_device
)

pico_add_extra_outputs(fight-key)
